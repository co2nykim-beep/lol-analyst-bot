# 구로야호 대회용 Discord LoL 봇 핸드오프

## 구현 완료 범위

기존 `/롤 전적`, `/롤 복기`, `/롤 조합`, `/롤 팁` 기능을 유지하면서 대회 전용 명령어를 추가했습니다. `/롤 전적`은 솔로 랭크·자유 랭크·일반 게임을 선택하고 최근 10경기를 분석하며, `/롤 팁`은 탑·정글·미드·원딜·서폿 라인을 선택합니다. 대회 데이터는 원본 경기 결과, 체크인 명단, 공개 전적 교차검증 기록을 바탕으로 `tournament_scouting.py`에서 명시적으로 관리합니다. 공식 본선 로스터가 아직 공개되지 않은 팀은 카드에 근거 상태를 표시하며, 자료가 없는 선수 계정을 임의로 연결하지 않습니다.

| 명령어 | 사용 예시 | 기능 |
|---|---|---|
| `/대회도움말` | `/대회도움말` | 대회용 명령어 사용법 |
| `/대회상대` | `/대회상대 아산드림윙즈` | 원본 경기·조합·핵심 선수·강점·약점 카드 |
| `/밴픽카드` | `/밴픽카드 아산드림윙즈 블루` | 3밴–3픽–2밴–2픽 초안과 실행 체크 |
| `/우리팀` | `/우리팀` | 구로야호의 초반·밴픽·한타 운영 원칙 |
| `/롤 전적` | `/롤 전적 게임이름 태그 모드` | 선택 모드 최근 10경기 분석 |
| `/롤 팁` | `/롤 팁 내챔피언 상대챔피언 라인` | 라인 선택형 라인전 팁 |

## 현재 포함된 상대 카드

TEAM 91, 아산드림윙즈, 도도도윤, 정정노이김, 빛과소금, EPG X PAI, 쇼다운, 김기연화이팅, KJJLK, 천재왕자 카드가 포함되어 있습니다. 팀별 카드에는 확인된 원본 경기 결과와 확정·부분확인 상태가 함께 표시됩니다.

## 배포 상태

코드는 GitHub 저장소 `co2nykim-beep/lol-analyst-bot`의 `main` 브랜치에 최신 수정 커밋으로 반영합니다. Render 서비스가 GitHub 자동 배포로 설정되어 있다면 새 커밋을 감지해 재배포합니다. 배포 로그에서 `Discord 연결 성공`과 슬래시 명령어 동기화 완료 로그를 확인해야 합니다.

## Riot API 키 갱신

기존 Discord 서버에서 확인된 핵심 장애는 Riot 개발 키 만료였습니다. 실제 키를 채팅이나 GitHub에 입력하지 말고 Render 환경변수의 `RIOT_API_KEY`를 새 값으로 교체한 뒤 재배포해야 `/롤 전적`과 `/롤 복기`가 다시 작동합니다. 전적 카드는 Riot Match-V5를 기본 근거로 사용하고, `EXTERNAL_SCOUTING_JSON`에 운영자가 직접 검증해 등록한 OP.GG·YOUR.GG·LOL.PS·FOW.LOL 메모만 보조 출처로 표시합니다. `GEMINI_API_KEY`, `DISCORD_BOT_TOKEN`, `GEMINI_MODEL`도 Render 환경변수에 유지되어야 합니다.

## 정책·기능 경계

대회 기능은 경기 전 밴픽 준비와 경기 종료 후 전적·복기만 지원합니다. 진행 중인 게임의 적 위치, 시야, 쿨다운, 비공개 이벤트를 수집하거나 실시간 전술 지시를 제공하지 않습니다. 이는 현재 저장소의 Riot 정책 경계와 기존 `REPLAY_REALTIME_ROADMAP.md`를 보존하기 위한 것입니다.

## 검증 결과

저장소 테스트는 `python3 -m unittest discover -s tests -v`로 실행했으며, 기존 회귀 테스트와 신규 대회 테스트를 포함해 **19개 테스트가 모두 통과**했습니다. 파이썬 구문 검사도 `python3 -m compileall -q bot.py riot_client.py gemini_analyzer.py tournament_scouting.py tests`로 통과했습니다.

## 다음 운영 절차

첫째, Render에서 `RIOT_API_KEY`를 갱신합니다. 둘째, Discord에서 `/대회도움말`, `/대회상대 아산드림윙즈`, `/밴픽카드 아산드림윙즈 블루`를 실행합니다. 셋째, 공식 로스터가 공개되면 새 Riot ID를 전달해 `tournament_scouting.py`의 확인 상태와 카드 내용을 갱신합니다.
