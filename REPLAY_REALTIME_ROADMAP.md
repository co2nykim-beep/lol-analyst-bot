# LoL Analyst Discord Bot — 리플레이·실시간 피드백 구현 로드맵

**작성자:** Manus AI  
**기준일:** 2026-08-15  
**적용 저장소:** `co2nykim-beep/lol-analyst-bot`

## 1. 이번 반영 범위와 핵심 결론

이번 수정으로 봇은 **Riot ID → PUUID → 최근 Match-V5 5경기 → 플레이어 중심 정량 요약 → Gemini 맞춤 코칭 → Discord Embed 및 코칭 스레드**의 흐름을 갖추게 되었습니다. 또한 최근 경기의 Match-V5 이벤트 타임라인을 사용해 사망·처치·개인 오브젝트 시점 중심의 사후 복기를 수행하는 `/롤 복기`와, 공개된 진행 중 게임 로스터의 챔피언 조합만으로 운영 조언을 제공하는 `/롤 인게임`을 추가했습니다.

> **핵심 구분:** `/롤 복기`는 Riot의 공식 Match-V5 타임라인을 해석하는 **경기 종료 후 데이터 복기**입니다. `.rofl` 영상 파일을 직접 재생하거나 경기 장면을 판독하는 기능은 아니며, Render에 배포된 봇이 공식 Riot API만으로 실시간 전투 이벤트를 수신하는 것도 아닙니다.

Riot는 Match-V5, Spectator-V5 등으로 전적과 진행 중 게임의 공개 정보를 제공하지만, 게임 세션 중 플레이어가 알 수 없는 정보를 통해 경쟁 우위를 주는 제품을 금지합니다. 따라서 이 프로젝트의 **권장 MVP는 사후 복기와 조합 중심의 온디맨드 코칭**이며, 자동 실시간 교전 지시나 적 위치·궁극기 쿨다운·시야 추론을 제공하는 기능은 구현 범위에서 제외해야 합니다.[1]

| 구분 | 현재 구현 상태 | 데이터 원천 | 사용자 경험 |
| --- | --- | --- | --- |
| 최근 전적 분석 | 구현 완료 | Account-V1, League-V4, Match-V5 | `/롤 전적 게임이름 태그`로 최근 솔로 랭크 5경기 카드와 코칭 스레드 생성 |
| 경기 종료 후 복기 | 구현 완료 | Match-V5, Match Timeline | `/롤 복기`로 최근 1경기 또는 Match ID를 사망·처치·오브젝트 시점 중심으로 복기 |
| 진행 중 조합 코칭 | 구현 완료 | Summoner-V4, Spectator-V5 | `/롤 인게임`으로 공개 챔피언 조합에 대한 역할·한타·오브젝트 플랜 안내 |
| `.rofl` 리플레이 판독 | 미구현 | 사용자가 보유한 리플레이 파일 | 별도 업로드 파이프라인 및 패치 호환 파서가 필요 |
| 실시간 이벤트 피드백 | 미구현·정책 검토 필요 | 공식 원격 API에는 스트리밍 이벤트 없음 | 로컬 동반 앱, 동의, 정책 재검토가 선행되어야 함 |

## 2. 반영한 코드 구조

`bot.py`는 Discord 슬래시 명령, 이벤트, Embed 구성, Render 헬스체크만 담당합니다. API 요청·전적 파싱·타임라인 이벤트 추출은 `riot_client.py`로, 프롬프트 작성·Gemini 호출·코칭 대화 세션은 `gemini_analyzer.py`로 분리했습니다. Riot ID는 현재 권장되는 `게임 이름#태그` 형식으로 Account-V1에서 PUUID를 조회합니다.[2]

| 파일 | 책임 | 이번 핵심 변경 |
| --- | --- | --- |
| `riot_client.py` | Riot HTTP 세션, 계정/랭크/전적/타임라인 수집 및 정규화 | `get_recent_matches(puuid, count=5)`, `get_recent_performance`, `get_match_timeline`, `get_match_review`, 챔피언 아이콘 URL 제공 |
| `gemini_analyzer.py` | 코칭 리포트와 세션형 질의응답 | 최근 5경기 JSON 기반 맞춤 분석, 타임라인 기반 복기 분석, 고정 형식의 한줄 피드백 분리 |
| `bot.py` | Discord 표현 계층과 명령 바인딩 | `/롤 전적`, `/롤 복기`, `/롤 인게임`, `/롤 팁`, Embed 카드, 코칭 스레드, 종료 시 HTTP 세션 정리 |
| `tests/test_core.py` | 회귀 검증 | 최근 전적 집계, 타임라인 이벤트 추출, 한줄 피드백 및 Embed 핵심 지표 검증 |
| `requirements.txt` | 배포 의존성 | 실제 사용 패키지만 유지하고 미사용 `groq` 제거 |

Gemini는 요청하신 `gemini-3.6-flash`를 기본 모델로 유지했습니다. 응답은 첫 줄을 `한줄 피드백:`으로 고정해 Embed의 짧은 진단으로 분리하고, 나머지는 Markdown 코칭 리포트로 표시합니다. Google Gen AI SDK는 JSON 스키마 기반 구조화 출력을 지원하므로, 다음 단계에서 세션별 코칭 결과를 DB에 저장하거나 대시보드로 전송할 때는 리포트를 JSON 스키마로 전환하는 것이 안전합니다.[3]

## 3. 현재 명령어 흐름

| 명령어 | 입력 | 결과 | 제한 사항 |
| --- | --- | --- | --- |
| `/롤 전적` | 게임 이름, 태그 | 최근 솔로 랭크 5경기의 승률, 평균 KDA, CS/분, 주 챔피언, 티어, 한줄 피드백 및 상세 코칭 | 표본이 5경기로 제한되므로 장기 실력 판정이 아님 |
| `/롤 복기` | 게임 이름, 태그, 선택 Match ID | 최근 경기의 결과·KDA·딜량·첫 사망·타임라인 이벤트를 이용한 사후 복기 | 타임라인이 제공되지 않는 경기에서는 결과 지표만 사용 |
| `/롤 인게임` | 게임 이름, 태그 | 공개 챔피언 로스터 기반의 역할·조합·한타/오브젝트 조언 | 적 위치, 쿨다운, 전투 이벤트 등 비공개 실시간 정보는 제공하지 않음 |
| `/롤 팁` | 내 챔피언, 상대 챔피언 | 라인전 중심의 일반 코칭 | 최신 패치 수치나 승률을 단정하지 않음 |

## 4. 리플레이·실시간 피드백의 실행 선택지

리플레이 기능은 요구하는 입력과 지연 시간에 따라 설계가 완전히 달라집니다. 특히 자동 피드백이 실제 경기 중 전술적 우위를 제공하지 않도록 데이터 범위와 메시지 빈도를 제한해야 합니다. 아래 세 가지 안은 모두 실현 가능하지만, 현재 봇에는 **A안이 이미 반영되어 있으며 B안이 다음 권장 단계**입니다.

| 접근 방식 | 작동 방식 | 장점 | 제약·트레이드오프 | 비용·설정 난이도 |
| --- | --- | --- | --- | --- |
| **A. 공식 데이터 사후 복기** | 경기 종료 후 Match-V5와 Timeline을 분석하고 Discord로 리포트 전송 | 가장 안정적이며 공식 API 범위 안에서 운영 가능; 현재 구현 완료 | 영상 장면, 마우스 이동, 스킬 적중은 분석 불가 | 추가 인프라 없음; 낮음 |
| **B. 사용자 업로드 영상 복기** | 사용자가 경기 종료 후 MP4 클립 또는 스크린샷을 Discord에 올리면 비동기 분석 | 실제 장면 기준의 실수·교전·웨이브 분석 가능 | 파일 용량, 개인정보, 분석 대기 시간, 업로드 동의와 저장 정책 필요 | 파일 저장소·작업 큐 필요; 중간 |
| **C. 로컬 동반 앱의 제한형 실시간 알림** | 사용자 PC의 허용된 공개 화면/클라이언트 정보만 수집해 본인에게 알림 | 즉시성이 높음 | 정책·권한·배포·패치 호환성 부담이 매우 크며, 게임 세션 특정 우위 정보를 주면 안 됨 | 별도 데스크톱 앱과 지속 실행 서비스 필요; 높음 |

### A안: Match-V5 타임라인 복기 고도화 — 다음 스프린트 권장

현재 `/롤 복기`는 이미 동작합니다. 다음 단계에서는 `build_match_review_data()`를 확장해 10분·15분 프레임의 CS/골드 변화, 첫 사망 전후의 아이템 구매, 드래곤·바론 전 사망, 연속 데스 구간을 규칙 기반으로 계산하면 됩니다. Gemini에는 원본 이벤트 전체가 아니라 **우선순위가 계산된 5~10개의 장면 후보**만 전달해야 코칭의 일관성과 비용을 함께 관리할 수 있습니다.

| 작업 | 산출물 | 완료 기준 |
| --- | --- | --- |
| 타임라인 파서 확장 | 10/15분 CS·골드 스냅샷, 오브젝트 전후 사망 플래그 | Match ID 10개에서 누락 없이 JSON 생성 |
| 규칙 엔진 | `early_death`, `objective_death`, `low_cs` 등 코칭 태그 | 태그마다 근거 타임스탬프를 보존 |
| `/롤 복기` 선택 UX | 최근 5경기 선택 또는 Match ID 자동완성 | 사용자가 최근 경기 중 하나를 명확히 선택 |
| 리포트 저장 | 소환사·Match ID·분석 버전·리포트의 최소 저장 | 같은 매치 재분석 시 캐시 사용 |

### B안: 영상 리플레이 업로드 분석 — 실제 장면 분석을 원할 때

`.rofl`은 클라이언트용 리플레이 형식이므로, Render 서버가 이를 안정적으로 장면 단위로 해석할 수 있다는 전제를 두면 안 됩니다. 패치마다 형식 또는 재생 호환성이 달라질 수 있고, 공식 Riot 문서는 리플레이 API를 카메라 제어 용도로 설명합니다.[4] 실사용 기능으로는 사용자가 **경기 종료 후 MP4 클립 또는 특정 장면 스크린샷**을 업로드하는 방식이 더 안전하고 예측 가능합니다.

이 방식은 Discord 첨부 파일을 수신한 뒤, 파일 크기·형식·보관 동의를 확인하고, 객체 저장소에 임시 보관한 다음, 프레임 추출 작업과 모델 분석을 비동기로 처리해야 합니다. 리포트에는 반드시 분석 근거가 된 `mm:ss`, 장면 설명, 개선 행동을 함께 표시해야 하며, 원본 파일은 예를 들어 24시간 후 삭제하는 보존 정책을 적용해야 합니다. 이 기능은 현재 소규모 Render 봇 프로세스 안에 직접 넣기보다, 업로드 큐와 별도 워커를 둔 구조가 적합합니다.

### C안: 실시간 피드백 — 구현 전 필수 경계

공식 Riot API의 Spectator-V5는 진행 중 게임 조회에 활용할 수 있지만, 이 프로젝트가 원격 봇만으로 장면별 이벤트를 스트리밍받는 기능을 제공하지는 않습니다. 또한 Riot 정책은 게임 클라이언트에 존재하지 않는 정보를 사용해 경쟁 우위를 제공하는 기능을 금지하고, 개인 키의 승인 가능한 예시로는 사전 공개 정보 기반 오버레이와 자기 전적 도구를 제시합니다.[1] 따라서 **적 위치, 스킬 쿨다운, 시야, 다음 행동을 실시간으로 지시하는 시스템은 만들지 않습니다.**

실시간성을 검토해야 한다면, 사용자가 명시적으로 동의한 로컬 동반 앱에서 플레이어 본인의 화면에 이미 나타난 정보만 읽고, 게임 중에는 전술 지시 대신 `게임 종료 후 자동 복기 예약` 또는 `휴식 알림` 같은 비경쟁적 알림만 제공하는 범위로 시작해야 합니다. 실제 배포 전에는 Riot Developer Portal에 제품을 등록하고 현재 정책 및 허용 범위를 재검토해야 합니다.[1]

## 5. 권장 실행 순서

첫 번째 배포에서는 이번 코드의 `/롤 전적`, `/롤 복기`, `/롤 인게임`을 실제 Discord 서버에서 검증합니다. Riot 개발 키가 만료되는 경우를 대비해 키 교체 절차를 문서화하고, Discord Developer Portal에서 **Message Content Intent**를 활성화해야 코칭 스레드의 일반 대화 기능이 작동합니다.

두 번째 스프린트에서는 사후 복기의 데이터 품질을 높입니다. 우선 타임라인 기반 규칙 3개(10분 CS 추세, 오브젝트 전 사망, 연속 데스)를 만들고, Gemini 리포트가 각 조언에 타임스탬프 근거를 남기는지 테스트합니다. 이 단계가 안정화되기 전에는 영상 업로드나 로컬 실시간 기능을 추가하지 않는 것이 좋습니다.

세 번째 스프린트에서 실제 영상 분석 수요가 확인되면, 업로드 동의·임시 저장·삭제 정책을 갖춘 클립 분석 기능을 별도 작업자로 추가합니다. 이때는 사용자별 리포트 조회 화면이나 분석 상태 메시지가 필요하므로, 단순 Discord 봇보다 데이터베이스와 파일 저장소를 갖춘 서비스 구조로 전환합니다.

## 6. 배포 체크리스트

| 점검 항목 | 필요한 조치 |
| --- | --- |
| 환경 변수 | `DISCORD_BOT_TOKEN`, `GEMINI_API_KEY`, `GEMINI_MODEL=gemini-3.6-flash`, `RIOT_API_KEY`를 Render 환경 변수에 설정 |
| Discord 권한 | 초대 URL에 `bot`, `applications.commands` 스코프를 포함하고 메시지 전송·스레드 생성·스레드 메시지 전송 권한 부여 |
| Discord 인텐트 | Developer Portal에서 **Message Content Intent** 활성화 |
| Render 시작 명령 | Procfile의 `web: python bot.py` 유지 |
| 보안 | `.env`는 절대 커밋하지 않고, 키가 노출된 적이 있다면 즉시 재발급 |
| Riot 정책 | 제품 등록·면책 문구·개인 키의 사용 범위를 최신 정책과 대조 |
| 검증 | `python3 -m unittest discover -s tests -v` 후 `/롤 전적`, `/롤 복기`, `/롤 인게임`을 순서대로 수동 확인 |

## 참고 자료

[1]: https://developer.riotgames.com/docs/lol "Riot Games Developer API Policy — League of Legends"
[2]: https://developer.riotgames.com/docs/lol "Riot ID에서 PUUID 조회 권장 방식"
[3]: https://ai.google.dev/gemini-api/docs/structured-output "Google Gemini API — Structured outputs"
[4]: https://developer.riotgames.com/docs/lol "Riot Games Developer Portal — Replay API 및 League Director 설명"
